# Private product planning (not for public repo)
v2/

# Secrets
.env
.env.*.local
*.pem
*.key

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.Python
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Terraform
.terraform/
*.tfstate
*.tfstate.*
# Commit infra/**/.terraform.lock.hcl for provider pins
infra/terraform/envs/*/*.auto.tfvars
infra/terraform/envs/*/*.tfvars
!infra/terraform/envs/*/*.tfvars.example
infra/terraform/envs/*/backend.hcl
infra/terraform/bootstrap/terraform.tfvars
**/tfplan
*.tfplan

# Docker / local volumes (if added later)
*.log.local

# Local FAISS index + chunk metadata (Phase 3 RAG)
.rag_index/

# API triage audit (append-only JSONL; may contain alert text)
data/logs/triage_outputs.jsonl
data/logs/n8n_workflow_events.jsonl
data/logs/triage_feedback.jsonl

# Eval Markdown reports (regenerate with triage-eval --out)
data/eval/reports/*
!data/eval/reports/.gitkeep
_split_backup/
