# Used when you run `next build` locally without deploy_frontend_cdn.sh.
# Deploy script sets NEXT_PUBLIC_API_BASE_URL from Terraform. For prod ALB:
#   terraform -chdir=../infra/terraform/envs/prod output -raw alb_url
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
