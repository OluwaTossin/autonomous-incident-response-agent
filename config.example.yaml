# Optional product tuning (non-secrets). Copy to e.g. `config.yaml` and set:
#   export CONFIG_YAML=config.yaml
# Environment variables override values here for the same key.
#
# Keys must match the environment variable names used by the app (UPPER_SNAKE).

# WORKSPACE_ID: default
# WORKSPACES_ROOT: workspaces
# AIRA_DATA_MODE: demo
# RAG_WORKSPACE_ONLY: 0
# RAG_CORPUS_ROOT: data
# RAG_INDEX_DIR: .rag_index
# RAG_TOP_K: 8
# LLM_MODEL: gpt-4o-mini
# LLM_TEMPERATURE: 0.2
# EMBEDDING_MODEL: text-embedding-3-small
# API_RATE_LIMIT_TRIAGE: "10/minute"
# API_RATE_LIMIT_INGEST: "30/minute"
# API_RATE_LIMIT_ADMIN_READ: "120/minute"
# API_RATE_LIMIT_ADMIN_UPLOAD: "20/minute"
# API_RATE_LIMIT_ADMIN_REINDEX: "2/minute"
# ADMIN_UPLOAD_MAX_BYTES: 5242880
