# Optional: for pip-only installs. Prefer: uv sync (see README).
# Phase 7 UI: pip install "gradio>=5" (or uv sync --extra ui)
openai>=1.40.0
faiss-cpu>=1.8.0
numpy>=1.26.0
python-dotenv>=1.0.0
fastapi>=0.115.0
python-multipart>=0.0.9
uvicorn[standard]>=0.32.0
